# Whitelist-only: ignore everything except *.md, *.py, *.jpg, *.vac, *.json, *.cs,
# *.cslist, and *.txt
# (`*` ignores; `!*/` lets Git traverse directories so `!` rules can match.)
*
!*/
!.gitignore
!**/*.md
!**/*.py
!**/*.jpg
!**/*.vac
!**/*.json
!**/*.cs
!**/*.cslist
!**/*.txt
